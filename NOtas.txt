🧠 Informe: Cómo una página web ejecuta código de un lenguaje de programación en el navegador
1. Introducción

Tradicionalmente, los programas escritos en lenguajes como C, C++, Python o Rust necesitan ser compilados e interpretados por software instalado localmente en el sistema operativo (por ejemplo, un compilador o intérprete).
Sin embargo, gracias a WebAssembly (WASM), es posible ejecutar código de alto rendimiento directamente en el navegador web, sin instalar nada adicional.

2. ¿Qué es WebAssembly?

WebAssembly (WASM) es un formato de código binario que puede ejecutarse dentro del navegador a gran velocidad.
Fue creado para permitir que lenguajes como C, C++, Rust, Go o Python puedan funcionar dentro del navegador como si fueran código nativo, pero aislados y seguros.

Características principales:

🔹 Corre dentro de todos los navegadores modernos (Chrome, Firefox, Edge, Safari).

🔹 Es seguro: se ejecuta dentro del sandbox del navegador.

🔹 Es rápido: se ejecuta casi tan rápido como un programa compilado nativamente.

🔹 Es portable: funciona igual en Windows, Linux, macOS y Android.

3. Funcionamiento general

Cuando visitas una página web que compila o ejecuta código, el proceso ocurre así:

🔸 1. El usuario abre la página web

Solo necesita un navegador moderno.
No instala compiladores, intérpretes ni entornos de desarrollo.

🔸 2. El navegador descarga los archivos del sitio

El HTML de la página incluye un módulo WebAssembly (.wasm) o una librería JavaScript (como Pyodide, Emscripten o wasm-pack).

Por ejemplo:

<script src="https://cdn.jsdelivr.net/pyodide/v0.26.2/full/pyodide.js"></script>

🔸 3. Se carga el entorno WASM

El navegador descarga el archivo .wasm y lo ejecuta dentro de un entorno de ejecución (runtime) como si fuera una mini máquina virtual.

Ejemplo para Python:

let pyodide = await loadPyodide();


Esto carga Pyodide, una versión de Python compilada a WebAssembly.

🔸 4. Se ejecuta el código del usuario

El código escrito por el usuario se pasa al motor WASM:

let result = await pyodide.runPython("print('Hola desde Python')");


El resultado se devuelve a JavaScript y se muestra en la web.

4. Ejemplo simplificado (flujo visual)
[Navegador del usuario]
        ↓
[HTML/JS de la página]  →  carga  →  [Motor WebAssembly (Pyodide, Emscripten...)]
        ↓
[Código del usuario]   →  ejecutado dentro del motor
        ↓
[Salida o resultado en pantalla]

5. ¿Qué hace cada tecnología?
Tecnología	Función
WebAssembly (WASM)	Permite ejecutar binarios seguros y rápidos en el navegador.
Emscripten	Compila C/C++ a WebAssembly.
Rust + wasm-pack	Compila proyectos Rust a módulos WebAssembly.
Pyodide	Convierte Python a WebAssembly para ejecutarlo en el navegador.
JavaScript	Sirve de puente entre el navegador y el motor WASM (envía y recibe código, resultados, etc.).
6. Ventajas

✅ No requiere instalación de compiladores o intérpretes.
✅ Portabilidad total: funciona igual en cualquier dispositivo.
✅ Aislamiento y seguridad.
✅ Ejecución rápida (casi nativa).
✅ Ideal para crear editores, compiladores, entornos de prueba, y laboratorios interactivos en la web.

7. Limitaciones

⚠️ No puede acceder directamente al sistema de archivos del usuario (por seguridad).
⚠️ La carga inicial puede ser más lenta (descarga del runtime).
⚠️ No todos los paquetes o librerías de escritorio funcionan igual en el navegador.

8. Conclusión

Una página web que ejecuta código (por ejemplo, Python, C o Rust) no compila realmente en el servidor, sino que utiliza WebAssembly para ejecutar el lenguaje dentro del navegador.
El usuario no necesita instalar nada porque el entorno ya está integrado en la web.

Este enfoque representa un gran avance en la educación, el desarrollo y la demostración de código en línea, permitiendo crear entornos de programación universales, seguros y rápidos, completamente basados en la web.